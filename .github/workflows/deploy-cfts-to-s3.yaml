name: Workflows
on:
  workflow_call:
    inputs:
      S3_BUCKET:
        required: true
        type: string
      AWS_ROLE:
        required: true
        type: string
      AWS_REGION:
        required: true
        type: string
jobs:
  deploy-aws-cfts:
    name: Deploy IAC to S3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Assume Role
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ github.event.inputs.AWS_ROLE }}
          aws-region: ${{ github.event.inputs.AWS_REGION }}
          role-duration-seconds: 1200
      - name: Push Files
        run: |          
          aws s3 cp cloud-native-iac/AWS/*.yaml s3://${{ github.event.inputs.S3_BUCKET }}